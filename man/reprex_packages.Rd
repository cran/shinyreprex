% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reprex_lockfile.R
\name{reprex_packages}
\alias{reprex_packages}
\title{Packages Required to Reproduce Reactives}
\usage{
reprex_packages(..., session = shiny::getDefaultReactiveDomain())
}
\arguments{
\item{...}{One or more \code{shiny::reactive} objects to inspect. If none are
supplied, every reactive registered against \code{session} by \code{\link[=register_reactives]{register_reactives()}}
is used.}

\item{session}{The Shiny session to read registered reactives from. Only used
when \code{...} is empty. Defaults to the current reactive domain.}
}
\value{
A character vector of unique package names. Base packages are excluded, as
they require no installation.
}
\description{
Extract the set of non-base packages needed to reproduce one or more
\code{shiny::reactive} objects, with duplicates removed across every reactive
passed.

Use this when building a custom UI around \code{\link[=reprex_lockfile]{reprex_lockfile()}}: it gives you
the list of detected packages to present to the user for selection.

Packages are found by reading the expression held in each reactive rather
than by generating its script, so every branch of an \code{if} or \code{switch}
contributes. The result may therefore be a superset of the \code{library()} calls
\code{\link[=reprex_reactive]{reprex_reactive()}} emits for the branch actually taken, on the basis that a
lockfile is safer holding a package that is not needed than missing one.
}
\examples{
\dontshow{if (rlang::is_installed("shiny")) withAutoprint(\{ # examplesIf}
library(shiny)

numeric_iris <- reactive(purrr::keep(iris, is.numeric))
styled_code <- reactive(styler::style_text("1 + 1"))

# Outside a running application, isolate supplies the reactive context
isolate(reprex_packages(numeric_iris, styled_code))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=reprex_lockfile]{reprex_lockfile()}} to turn this set into an \code{renv} lockfile, and
\code{\link[=register_reactives]{register_reactives()}} to record reactives from within each module.
}

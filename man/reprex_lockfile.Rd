% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reprex_lockfile.R
\name{reprex_lockfile}
\alias{reprex_lockfile}
\title{Create a Lockfile to Reproduce Reactives}
\usage{
reprex_lockfile(
  ...,
  packages = NULL,
  lockfile = "renv.lock",
  exclude = NULL,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{...}{One or more \code{shiny::reactive} objects to reproduce. If none are
supplied, every reactive registered by \code{\link[=register_reactives]{register_reactives()}} is used, so a
modular application can offer a whole-app lockfile without threading
reactives up to the top level.}

\item{packages}{Character vector of package names to snapshot. If \code{NULL}
(the default), every package detected across \code{...} is used. Supplying this
lets the user narrow the set, or add a package the detector could not find
(for example one attached only for an operator or method).}

\item{lockfile}{Path to write the lockfile to. Defaults to \code{"renv.lock"}.}

\item{exclude}{Character vector of package names to omit from the snapshot.}

\item{session}{The Shiny session to read registered reactives from. Only used
when \code{...} is empty and \code{packages} is \code{NULL}.}
}
\value{
The absolute path the lockfile was written to, invisibly.
}
\description{
Capture the exact package versions, sources and R version needed to reproduce
one or more \code{shiny::reactive} objects, as an \code{renv} lockfile. Restoring the
lockfile with \code{\link[renv:restore]{renv::restore()}} recreates the environment the reactives were
generated in, including the recursive dependency tree.

A single lockfile covers every reactive passed, so an application can offer
one download that reproduces all of its outputs.
\subsection{Snapshot Scope}{

The snapshot is taken from the currently loaded library (\code{.libPaths()}), so
the versions recorded are exactly those the running Shiny session used to
produce the reactives. \code{renv} resolves and records the full recursive
dependency tree of \code{packages}, so only the top-level set needs to be supplied.
}

\subsection{Isolation}{

The snapshot runs against a throwaway temporary project, so it never writes
\code{renv} infrastructure into, or otherwise modifies, the application's own
directory.
}

\subsection{Output Path}{

A relative \code{lockfile} is resolved against the working directory, not against
the temporary project used for the snapshot. The default therefore writes
\code{renv.lock} into whichever directory the application is running from. Pass an
absolute path to control where it lands; inside a
\code{\link[shiny:downloadHandler]{shiny::downloadHandler()}} that is the \code{file} argument.
}
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
library(shiny)

numeric_iris <- reactive(purrr::keep(iris, is.numeric))

isolate(reprex_lockfile(numeric_iris, lockfile = tempfile(fileext = ".lock")))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=reprex_packages]{reprex_packages()}} to inspect the detected set without writing a
lockfile, and \code{\link[=register_reactives]{register_reactives()}} to record reactives from within each module.
}

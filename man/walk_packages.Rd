% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/walk_packages.R
\name{walk_packages}
\alias{walk_packages}
\title{Collect Packages From an Expression}
\usage{
walk_packages(expr, env, seen = character(), packages = character())
}
\arguments{
\item{expr}{An expression to walk}

\item{env}{The environment the expression belongs to, used to identify calls
to other reactives}

\item{seen}{Names of reactives already walked, preventing a reactive that is
referenced more than once from being walked repeatedly}

\item{packages}{Packages collected so far}
}
\value{
A character vector of unique package names, excluding base packages.
}
\description{
Walk an expression and collect the packages of every call found within it,
without reproducing any of the code. This is a deliberately cheap alternative
to \code{\link[=repro_chunk]{repro_chunk()}} for the cases that only need the package list, such as
\code{\link[=reprex_packages]{reprex_packages()}} and \code{\link[=reprex_lockfile]{reprex_lockfile()}}.
\subsection{Why Not Reproduce the Code}{

Reproducing a chunk substitutes reactive and input values into the
expression, which means constructing every module variable as code via
\code{constructive::construct}. For a module holding a data frame that dominates
the run time, and all of it is discarded when only the packages are wanted.
}

\subsection{Branches Are Not Evaluated}{

Every branch of an \code{if} or \code{switch} is walked, rather than evaluating the
condition and following only the branch that would be taken. A lockfile is
safer for containing a package that turns out not to be needed than for
missing one, and not evaluating keeps the walk both faster and independent of
the current input values.
}
}
\keyword{internal}

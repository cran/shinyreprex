% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reactive_expression.R
\name{reactive_expression}
\alias{reactive_expression}
\title{Reactive Expression and Environment}
\usage{
reactive_expression(x)
}
\arguments{
\item{x}{A \code{shiny::reactive} object}
}
\value{
A list with two elements: \code{body}, the unevaluated body of the reactive, and
\code{env}, the module environment the reactive was assigned in.
}
\description{
Extract the unevaluated body of a reactive, along with the environment it was
assigned in.
\subsection{Environments}{

The \code{Observable} object attached to the given reactive is extracted. Within the
\code{Observable}, the \code{.origFunc} contains the environment that the reactive expression
was created - the parent environment being the module that the reactive is assigned
in. This allows the variables in the module to be found and set as pre-requisites
for the given reactive.

If \code{bindCache} or \code{bindEvent} are used, then the environment found is the call within
the relevant function. To get to the module environment, we find that the \code{reactive}
is assigned as "\code{wrappedFunc}", so that is used to find the module environment.
}
}
\keyword{internal}
